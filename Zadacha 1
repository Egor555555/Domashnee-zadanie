//Ученики одной из школ увлеклись изготовлением подарков для малышей одного детского дома. В первый свой визит они подарили малышам X подарков. 
//Во второй раз подарили в 2 подарка больше, в третий на 3 подарка больше, чем в предыдущий раз. 
//И так далее, в свой K-ый визит они принесли на K подарков больше, чем в предыдущий. 
//Сколько подарков они принесут в N-ый раз. Сколько всего подарков будет принесено ими за все N визитов.
#include "pt4.h"
using namespace std;

void Solve()
{
    Task("TheTwo9");
    int n, x, s;
    pt >> n >> x;
    s = x;
    for (int i = 2; i <= n; i++)
    {
        s += x + i;
        x = x + i;
    }
    pt << x << s;
}

